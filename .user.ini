open_basedir=/www/wwwroot/agencia.maiolidesign.com.br/:/tmp/

; Tempo máximo de execução — necessário para chamadas longas às APIs de IA
max_execution_time = 300

; Tempo máximo de input (deve ser >= max_execution_time)
max_input_time = 300